var addMusicView = document.getElementById('AMV');
//Hides the addMusicView on pageload
function hideBox (div) {
  div.hidden = true;
}; hideBox(addMusicView);
//

function addViewButtons (navLinks) {
  for (var i = 0; i < navLinks.ul.li.length; i++);
    console.log('hi');
};
addViewButtons(document.querySelector('nav'))